test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

cloudinary:
  service: Cloudinary
  folder: <%= Rails.env %>

# local:
#   service: Disk
#   root: <%= Rails.root.join("storage") %>
